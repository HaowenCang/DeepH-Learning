# M9 source_build v2 修复及单次编译授权记录

记录日期：2026-08-31。任务：`019fc7b8-cd95-7d20-84d0-229e2a73749e`。本日期为记录制作日期，不推定用户消息的精确时刻。

v1独立安装审计发现 `M9-SB-V1-INSTALL-B01` 后，主agent请求：“保留 v1 和旧凭据权限，建立 v2 修复门控，经独立实施及安装审计通过后执行一次冻结编译与自测。”用户随后明确回复“批准”。该批准是本轮新的修复及单次执行依据，不是自动goal续行消息的推定授权。

允许主agent在新版本路径实施v2权限分层修复、必要的合成回归测试和只读核验；独立子agent完成实施审计及安装事实审计。保留v1安装、旧代码、旧gate、旧报告、FAIL证据与预算历史；不放宽8项root-only历史对象或root-private证据的权限，不就地改写任何旧冻结来源。

独立实施及安装审计均达到PASS/BLOCKING=0/NON_BLOCKING=0，实际UID1000消费验证通过，且即时预算与存储预测符合原合同后，允许签发v2专用执行许可并执行一次冻结 `source_build`。固定operation为 `a237ec1f2b404d898840c643e9ae501b`，nonce为 `8034661a857747a62bfbe2c25fb825452aa4dfe7a3d949cda9478e56a18e2a94`。v1旧operation、gate、报告或许可不能充当v2权威。

本次完整构建范围保持不变：HDF5 1.12.1 configure、make、make check、install；OpenMX 3.9加官方3.9.9补丁的构建与受控源码检查；仅采用冻结overlap补丁的两个C文件构建overlap版本；检查产物允许清单、安装文件、动态库、日志和来源。库自测不是材料结构计算。

不包括下载、更换软件版本或材料体系、运行材料结构、生成overlap数据、smoke/batch、GPU、训练、Hamiltonian/SCF或其他DFT标签。D-018仅取消总墙钟期限；原CPU/GPU/存储上限、抵扣、累计用量及证据全部保留。本次使用GPU bucket=none、原overlap_build子预算及4294967296-byte存储预测，不增加额度或抵扣。

正式安装、许可或执行中出现非预期拒绝、异常、失败或超时，应当停止、封存现场并独立复核，不自动重放。预先指定的临时夹具负例及零写缺许可验证不属于正式动作失败，也不消费编译授权。安装未完成或无执行许可不能启动编译；执行成功或失败改变runtime后，旧许可均不能重用。

本记录和v2工作包在实施审计前进入新冻结manifest。后续结果另行记录，不回改已经绑定的授权或审计文件。
