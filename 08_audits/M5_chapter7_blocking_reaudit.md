# M5-04 第 7 章阻塞项独立定点复核

- 复核日期：2026-08-04
- 原审计：`08_audits/M5_chapter7_independent_content_audit.md`
- 定点对象：`M5-C7-B01`
- 修订文件：`03_textbook/chapters/07_scf_algorithms/examples.md`
- 修订文件 SHA-256：`4A98A7601E2225E5265E9FB25484F906E01ACBC995ED91FA780666152F83B803`
- 复核结论：`CLOSED`
- 新增 `BLOCKING`：0 项
- 剩余 `BLOCKING`：0 项
- 是否允许 M5-04 标记为 `COMPLETED`：**是**
- 是否允许启动 M5-05：**是**

本次仅复核原审计冻结的唯一阻塞 `M5-C7-B01` 及其要求的 Python/Pandoc 回归，不重新扩展第 7 章全量审计范围。除新增本报告外，未修改修订文件、原审计或其他材料。

## 1. M5-C7-B01 关闭证据

修订后的 T-C01 失败轨迹先在 `x_0` 记录初始残差，再令 `updates=12`，由 `range(updates)` 恰好执行 12 次更新；每次更新后追加新状态的残差。运行结束时：

| 检查项 | 结果 |
|---|---:|
| 实际循环更新数 | `12` |
| 残差条目数 | `13` |
| 独立状态数 | `13`，对应 `x_0,...,x_12` |
| 程序终态与独立 `x_12` 的最大绝对差 | `0` |
| 程序终态与独立 `x_13` 的 2-范数距离 | `231.36627628892171` |
| 程序残差轨迹与独立 `x_0,...,x_12` 轨迹的最大绝对差 | `0` |
| `r_0` | `0.37416573867739417` |
| `r_12` | `115.68313814446086` |
| `r_12/r_0` | `309.17619168815162` |

因此，程序终态明确为 `x_12`，没有执行或产生未记录的 `x_13`；`r_12/r_0>10` 的冻结强制拒绝条件成立。代码中的 `updates == 12` 与 `len(residuals) == 13` 断言也原样通过。原问题所指的第 13 次未记录更新已消除，`M5-C7-B01=CLOSED`。

## 2. 回归检查

### 2.1 四个 Python 块

使用冻结解释器 Python 3.12.13、NumPy 2.3.5，提取 `examples.md` 中恰好 4 个 `python` 围栏，并把每个围栏原样作为独立程序执行。四个程序均以退出码 0 结束，标准错误为空。除已关闭的更新计数问题外，未发现新的断言、数值或执行回归。

### 2.2 严格 Pandoc 与控制字符

对修订文件执行：

```text
pandoc --from=markdown+tex_math_dollars+tex_math_single_backslash \
       --to=html5 --mathml --fail-if-warnings \
       03_textbook/chapters/07_scf_algorithms/examples.md
```

退出码为 0。除 TAB、LF、CR 外的 C0 控制字符及 DEL 数量为 0，TAB 数量也为 0；代码围栏数量与闭合性正常。未发现由本次修复引入的 Markdown、MathML 或控制字符问题。

## 3. 定点门控结论

本次独立定点复核判定：

- `M5-C7-B01=CLOSED`；
- 新增 `BLOCKING=0`；
- 剩余 `BLOCKING=0`；
- **允许将 M5-04 标记为 `COMPLETED`；**
- **允许启动 M5-05。**

该结论只关闭原审计冻结的唯一阻塞项，并确认规定的回归检查通过；不授权修改 M8/M9 外部动作边界。
