# M6-05 第 14 章定点复核

## 1. 复核范围

本次复核由原独立审计员执行，只检查初审报告中的稳定问题 ID：

- `M6-05-B01`
- `M6-05-B02`
- `M6-05-N01`
- `M6-05-N02`

被复核材料限于第 14 章 `chapter.md`、`examples.md`、D-D05/D-D07、Q14 与 A14。执行了只读源码核查、严格 Pandoc/MathML 转换、数学节点计数、数学节点外 TeX 残留扫描、cutoff 条件回归、规范 JSON/SHA-256 独立复算，以及周期路径连续提升条件核查。未修改被审材料、计划、台账、README 或代码；Pandoc 未使用 `-o` 或 `--output`。

## 2. 逐项复核

### M6-05-B01：CLOSED

第 14 章五类材料中的行内数学已改为有效且配对的 `\(...\)`，例 14-5 的 `\quad` 与 D-D05 的 `\sigma_{\min}` 残缺也已修复。

只向标准输出执行 `pandoc --from=markdown+tex_math_single_backslash --to=html5 --mathml --fail-if-warnings`，结果如下：

| 材料 | 源文件数学开始定界符数 | MathML 节点数 | 数学节点外目标 TeX 残留 | Pandoc |
|---|---:|---:|---:|---|
| `chapter.md` | 77 | 77 | 0 | exit 0 |
| `examples.md` | 33 | 33 | 0 | exit 0 |
| D-D05/D-D07 | 44 | 44 | 0 | exit 0 |
| Q14 | 27 | 27 | 0 | exit 0 |
| A14 | 61 | 61 | 0 | exit 0 |

残留扫描在去除 MathML、代码块和行内代码后检查了 `mathbb`、`in`、`kappa`、`sigma`、`lVert/rVert`、`sqrt`、`times`、`pm`、`operatorname`、`quad`、`texttt`、`lceil/rceil`、`prod` 等命令，未发现数学外残留。原先渲染为普通文本的 D-D05 定义、Q14-02 不等式、A14-03 计数、例 14-5 和 D-D07 双射现在均生成 MathML。

### M6-05-B02：CLOSED

`chapter.md:58` 已将 cutoff 冻结为有限 float64 标量且严格大于 0；`chapter.md:265` 明确将 NaN、正无穷、负无穷和非正数列为硬失败。D-D05 开头同步声明相同定义域，Q14-10 明确要求分别覆盖五类失败输入，A14-10 的 T-D10 行给出相应成功条件与定向失败样例。

因此，正无穷不再能仅凭 `r_c>0` 进入 `ceil(r_c ||B_:k||+1)`；材料契约已足以指导后续 validator 使用 float64 有限性检查和严格正值检查。

### M6-05-N01：CLOSED

D-D07 `97-103` 已完整冻结规范边 ID：

- 字段顺序为版本、`structure_id`、`i`、`j`、`nx`、`ny`、`nz`；
- 版本和 `structure_id` 为字符串，其余字段为十进制整数；
- `ensure_ascii=false`；
- `separators=(",",":")`，无空格；
- UTF-8 编码后计算 SHA-256；
- shift 使用回拉后的规范整数。

独立复算 `["stageD-edge-v1","syn-14",0,1,-1,0,0]` 得到

```text
f83e3b238ba5c1b71cc773c08afa5fab22c35c1e38c6418b629fde616f5020e3
```

与例 14-8 和 D-D07 的回归锚点一致。

### M6-05-N02：CLOSED

`chapter.md:217-229` 已在结论原位补全局部消息、同步更新、无跨图边、初始特征未预编码全局结构、无全局读出或全局状态回灌等条件；`chapter.md:221` 还明确要求沿同一连续周期提升选择相邻边镜像，使路径端点代表连续。

正文与 A14-08 已区分：

- 长度不超过 L 的图路径给出 `L r_c` 几何上界；
- 几何距离小于该上界不推出存在 L 跳路径；
- 图结构可达不等于数值上实际非零影响；
- 预编码全局特征或全局状态回灌会越出该局部传播结论的适用范围。

三角不等式及其使用条件现已一致，原非阻塞项关闭。

## 3. 新增问题回归

- 新增 `BLOCKING = 0`
- 新增 `NON_BLOCKING = 0`
- 剩余 `BLOCKING = 0`
- 剩余 `NON_BLOCKING = 0`

定点复核未发现由本轮修订引入的新公式、数值、provenance 或授权边界问题。初审已经通过的 row-lattice、有限镜像盒、零自环/非零自镜像、多镜像、最小镜像充分条件、逐原子换胞双射、复杂度、轨道身份、批处理同步、来源边界及 M8/M9 授权边界仍保持不变。

## 4. 最终结论

**PASS**

`M6-05-B01`、`M6-05-B02`、`M6-05-N01` 和 `M6-05-N02` 全部关闭，新增与剩余问题均为 0。

**允许将 M6-05 标记为完成，并允许启动 M6-06。**
