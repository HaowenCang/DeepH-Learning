# M9 无总墙钟期限授权记录

## 授权事实

2026-08-20，用户明确批准：M9 改为无总墙钟期限；不重置或增加 CPU、GPU、存储预算，保留全部历史用量与证据。

## 决策解释

本授权只取消 D-013/D-014 原 604800 秒总墙钟上限及其绝对截止时间的执行约束。原始起点 `2026-08-11T14:50:05.7533015Z`、原始截止时间 `2026-08-18T14:50:05.7533015Z`、原始 604800 秒额度、全部 ledger 事件、raw CPU 用量、既有 CPU credit、GPU 用量、存储基线、失败事务、恢复事务、capability、gate 和独立审计证据必须保留，不得删除、回写、归零或伪装为未曾存在。

CPU 子预算保持 `overlap_build=7200 s`、`overlap_smoke=1800 s`、`overlap_batch=21600 s`；GPU 总预算保持 86400 秒及既有分桶；总存储保持 100 GiB，项目审计白名单保持 1 GiB，overlap 新增存储保持 10 GiB。取消总墙钟期限不得转化为新增 CPU、GPU 或存储额度。

## 执行边界

正式运行态不得手工编辑。应先完成 D-018 迁移合同、代码、测试、冻结清单和独立零问题审计；随后仅可通过一次性 `overlap-migrate-unlimited-wall-clock` 入口迁移预算状态与执行 gate。迁移执行后必须立即停止并进行独立事实审计。该迁移本身不授权 `source_prepare`、`source_build`、smoke、batch、GPU、Hamiltonian/SCF 标签或 M9-05；后续动作仍按既有 D-017 门控顺序逐项放行。
