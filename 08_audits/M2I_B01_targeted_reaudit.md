# M2I B-01 定点复核报告

## 1. 最终结论

**结论：通过。**

原独立审计报告 `08_audits/M2I_chapter1_independent_content_audit.md` 中的 B-01 已被完整关闭。`outline.md` 已同时加入可执行的端到端综合门控题和可保存、可由另一审阅者复核的验收量规；训练链、推理链、对象类型、逐箭头字段、SCF 边界、未被替代的数值步骤、方法角色、失败条件与修订记录要求均有明确文本依据。

NB-01—NB-04 均已正确落地。定点修改没有引入对象层级、数学条件、方法角色或阶段 A 教学门槛回归。本次复核未发现仍需保持的 `BLOCKING`。本报告可作为关闭 B-01 的内容审计证据；进度台账更新及后续门控状态变更不在本次只写审计报告的授权范围内。

被审计文件：`03_textbook/chapters/01_deeph_problem/outline.md`

SHA-256：`A5DFB8EE0EECA55066DB48E35FBEFD9128C22C17631B09656470ECD5887DCC16`

## 2. 审计范围与判据

本次审计是对既有独立审计结论的定点复核，不是对全部论文、PDF 和软件状态重新执行完整来源审计。直接读取并核对了以下文件：

- `08_audits/M2I_chapter1_independent_content_audit.md`，重点为 B-01（第 99—112 行）和 NB-01—NB-04（第 114—123 行）；
- `03_textbook/chapters/01_deeph_problem/outline.md` 全文；
- `03_textbook/chapters/01_deeph_problem/independent_audit_brief.md` 全文；
- `00_scope/learning_route.md` 阶段 A（第 7—37 行）；
- `03_textbook/chapters/01_deeph_problem/sources.md`，重点为原始 DeepH 的 `H/S` 分工、方法与软件证据边界及尚未关闭的版本级缺口；
- `03_textbook/notation.md` 全文。

判定依据为：B-01 的两项逐位置修改要求及其失败条件是否全部落实；NB-01—NB-04 是否在建议位置以不失真的方式落地；新增内容是否违反独立审计简报第 15—35 行的对象层级、计算边界、数学条件、表示约定、证据边界和方法角色要求；是否满足阶段 A 的综合任务与完成标准。

## 3. B-01 逐项证据与判定

| 核验项 | 当前证据定位 | 判定 |
|---|---|---|
| 分别交付训练链与推理链 | `outline.md:214-216, 280-283` 明确要求在不查资料条件下提交第一版训练链、推理链流程图，并设置独立“综合门控题” | 通过 |
| 节点对象类型完整 | `outline.md:217, 284` 要求至少区分算符、给定基中的矩阵、监督标签、模型预测、辅助输入、本征量和进一步物理量 | 通过 |
| 逐箭头字段可执行 | `outline.md:218, 285` 逐项要求输入、输出、固定约定、成立假设、可能误差和至少一种可执行检查或验证 | 通过 |
| 训练链关键节点完整 | `outline.md:94-101, 286` 包含结构、固定 DFT 设置、电子结构数据/辅助对象、标签、索引与图预处理，并明确监督对象取决于版本和任务 | 通过 |
| 推理链及辅助步骤完整 | `outline.md:103-121, 286` 包含新结构、周期原子图、预测矩阵、必要的重叠矩阵、Fourier 变换、矩阵组装、广义本征求解、占据和性质后处理 | 通过 |
| SCF 替代边界准确 | `outline.md:39, 119-121, 221, 287` 要求圈出原始 DeepH 在新结构上替代的 SCF 标签生成环节，同时明确不能把重叠、Fourier、组装、求解、占据或后处理写成被跳过 | 通过 |
| 方法角色进入验收 | `outline.md:198-208, 288` 要求定位原始 DeepH、HPRO、DeepH-dock、DeePTB 和 DeepH-R；不属于同一箭头时必须说明其改变标签获得路径、软件接口、学习目标或问题定义 | 通过 |
| 交付物与修订记录可复核 | `outline.md:214-219, 289` 要求保存第一版、修正版、错误或遗漏及逐项修正理由，并保留不少于 15 分钟的逐箭头说明记录 | 通过 |
| 失败条件和通过条件明确 | `outline.md:221, 291` 采用关键错误优先规则；对象混淆、错误统一化 `S` 的角色、错误扩大“跳过 SCF”范围或缺少关键箭头均直接不通过，且不得由局部得分抵消 | 通过 |

**B-01 判定：已完整关闭。** 原报告第 109—110 行要求的“综合题”和“可记录验收量规”均已存在，且原报告列出的关键失败条件没有被弱化。新增题目不是术语回忆题，而是具有交付物、字段约束、失败条件和审阅记录的端到端能力测试，满足 `learning_route.md:29-33` 的阶段 A 要求。

## 4. NB-01—NB-04 逐项判定

| 编号 | 当前证据定位 | 核验结果 | 判定 |
|---|---|---|---|
| NB-01 | `outline.md:129-140` | 已定义有序基矢行 `\boldsymbol\Phi=(|\phi_1\rangle,\ldots,|\phi_N\rangle)` 和 `\boldsymbol\Phi'=\boldsymbol\Phi A`；明确 `A\in\mathbb C^{N\times N}` 可逆、新旧基张成同一子空间，并给出 `c'=A^{-1}c`。三项变换方向相互一致 | 通过 |
| NB-02 | `outline.md:61-70` | 在广义本征式首次出现后即声明 `H(\mathbf k)` 厄米、`S(\mathbf k)` 厄米正定和 `S`-正交归一，并把简并子空间规范与病态 `S` 的处理明确后置 | 通过 |
| NB-03 | `outline.md:152-159, 202-204` | 协变式明确标为依赖已声明主动/被动约定的示意式；要求 `D_i,D_j` 与实/复球谐、轨道排序和基函数相位相容；具体约定后置到第 16—19 章，并与 DeepH-R 的实空间势输出对象交叉对照 | 通过 |
| NB-04 | `outline.md:92-101, 115-117` | `{H(\mathbf R),S(\mathbf R)}` 已标为“电子结构数据/辅助对象集合”；监督损失中的量取决于版本和任务；原始 DeepH 的监督目标为 `H`，`S` 由基函数内积获得并用于广义本征求解，且明确禁止外推到所有现代任务 | 通过 |

四项建议均保持了 `sources.md:48, 55-64` 的证据边界，也符合 `notation.md:26-28` 对基组、相位、主动/被动变换、实/复类型及成立条件的强制说明要求。

## 5. 回归检查

### 5.1 对象层级与计算边界

`outline.md:5-17, 43-70` 继续区分 Kohn–Sham 算符、给定基中的矩阵、监督标签、模型预测、重叠矩阵、本征量和进一步物理量。`outline.md:94-121` 没有把训练数据集合、模型输出和下游辅助对象合并为同一对象，也没有把“跳过 SCF”扩大为跳过全部电子结构步骤。综合题中的节点类型和关键错误规则进一步强化而非削弱了该区分。未发现对象层级回归。

### 5.2 数学条件与表示约定

`outline.md:49-55, 63-70` 保留了 `H,S` 的厄米性、线性无关基下 `S\succ0`、`S`-归一化及简并/病态边界；`outline.md:129-159` 的可逆一致基变换、Fourier 示例边界和旋转协变约定相互兼容；`outline.md:171-178` 仍将广义本征值一阶扰动限定于可微厄米矩阵族、正定 `S`、简单特征值、`S`-归一化和小扰动。未发现公式方向、成立条件或表示约定回归。

### 5.3 方法角色与证据边界

`outline.md:82-88, 188-208` 继续区分固定 AO 表示的 Hamiltonian 预测、简化 tight-binding 目标、PW→AO 标签表示桥接、软件接口与后处理、变分训练信号以及实空间 Kohn–Sham 势预测。`outline.md:288` 要求读者说明方法为何不属于同一箭头，未将 HPRO 或 DeepH-dock 错写为预测网络，也未将 DeepH-R 错写为 AO Hamiltonian 预测。`sources.md:60-64` 中仍未关闭的版本级轨道排序、Fourier 相位和现代包 `H/S` 分工问题没有被新增内容擅自确定。未发现方法角色或证据边界回归。

### 5.4 阶段 A 教学门槛

`learning_route.md:29-33` 要求端到端流程图、逐箭头输入/输出/假设/误差/验证、未替代的物理近似与成本、15 分钟口头解释及可执行检查。`outline.md:214-221, 280-291` 对这些要求逐一给出交付物、说明记录、审计表、关键失败条件和修订证据，且没有引入超出阶段 A 前置知识的 DFT 推导或网络实现要求。教学门槛已由能力声明转化为可复核任务，未发现难度错位或验收弱化。

## 6. 可复现只读验证

### 6.1 文件哈希

在项目根目录执行：

```powershell
Get-FileHash -LiteralPath '03_textbook/chapters/01_deeph_problem/outline.md' -Algorithm SHA256
```

得到：

```text
A5DFB8EE0EECA55066DB48E35FBEFD9128C22C17631B09656470ECD5887DCC16
```

### 6.2 关键落地点检索

以下命令只读取提纲，并返回关键落地点的行号：

```powershell
$p = '03_textbook/chapters/01_deeph_problem/outline.md'
$required = @(
  'H(\mathbf k)=H(\mathbf k)^\dagger',
  'S(\mathbf k)=S(\mathbf k)^\dagger\succ0',
  '\boldsymbol\Phi',
  'A\in\mathbb C',
  '电子结构数据/辅助对象集合',
  '综合门控题',
  '逐箭头审计表',
  '第一版、修正版和逐项修正理由',
  'HPRO、DeepH-dock、DeePTB 和 DeepH-R',
  '直接判定不通过'
)
$required | ForEach-Object {
  Select-String -LiteralPath $p -SimpleMatch -Pattern $_ -Encoding UTF8
}
```

本次运行中所有项目均命中；核心位置分别为第 68、132—134、97、280、218/282、289、288 和 291 行。

### 6.3 数学只读复算

采用 NumPy/SciPy 和固定随机种子 `20260803`，仅在内存中生成 `5\times5` 复厄米 `H`、正定 `S` 和条件适中的复可逆 `A`，复核一致基变换的谱不变性、`S`-正交归一以及 `\delta E=c^\dagger(\delta H-E\delta S)c` 的一阶余项。未生成项目文件。结果为：

| 检查 | 结果 |
|---|---:|
| 一致变换前后最大谱差 | `7.772e-16` |
| `\|C^\dagger SC-I\|_F` | `2.033e-15` |
| `S` 的最小特征值 | `2.105766` |
| `\kappa_2(A)` | `1.234120` |
| 一阶余项，`t=10^{-3}` | `4.520993e-07` |
| 一阶余项，`t=5\times10^{-4}` | `1.130137e-07` |
| 一阶余项，`t=2.5\times10^{-4}` | `2.825204e-08` |

步长连续减半时余项约缩小四倍，符合简单特征值、小扰动条件下的二阶余项行为；该结果支持提纲现有公式方向和条件，没有替代对简并、近简并或病态 `S` 的单独处理要求。

## 7. 门控判断

B-01 已关闭，NB-01—NB-04 已正确落地，且定点修改未引入回归。因此，本次复核结论为 **通过**，**不存在仍需保留的 `BLOCKING`**。是否据此更新 `progress_tracker.md` 或推进后续写作，应由获授权的后续任务另行执行；本次审计未修改任何被审计材料或进度文件。
