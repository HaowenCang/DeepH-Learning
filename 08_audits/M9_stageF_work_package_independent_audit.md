# M9 阶段 F 工作包正式独立审计

## 1. 正式结论

**结论：FAIL。** 本次审计记录 `BLOCKING=4`、`NON_BLOCKING=0`。在四项阻塞均按最小关闭条件修订，并由同一独立审计员定点复核为 `PASS`、`BLOCKING=0`、`NON_BLOCKING=0` 前，**不允许启动 M9-02，不允许安装系统包、Python/Julia 环境或 DeepH-pack，也不允许下载正式数据或运行复现实验**。

冻结的源码标签与提交、graphene 数据对象、本机 WSL 路径、GPU 身份、直接依赖候选、无凭据落盘边界、OpenMX/DFT 禁止项和顺序停止原则总体自洽。失败原因不在这些对象本身，而在四个可局部修复的执行合同缺口：M9 授权状态尚未在权威决策链中闭合；7 天、24 GPU 小时和 100 GiB 缺少可执行的统一计量合同；阶段 F 主计划要求的适用旋转、反演或时间反演验收没有进入 M9-06；数值容差冻结时点被工作包从“训练前”放宽到了“最终测试前”。

## 2. 审计范围、约束与输入快照

本次只读审计覆盖下列七个输入。除新增本报告外，没有修改任何被审材料；没有安装软件、下载正式数据、克隆源码或运行训练。只读取了不含认证信息的 WSL/宿主状态和公开官方元数据；没有读取、索取、推断或记录任何认证口令。

| 输入 | 字节数 | SHA-256 |
|---|---:|---|
| [`M9_stageF_work_package.md`](M9_stageF_work_package.md) | 7,571 | `F35D0611437BE2F3F9CD12A0DFE0861B57374DF28BA3FFB5F51A6FE973019EAF` |
| [`decisions.md`](../decisions.md) 的 D-013 | 13,539 | `428BC2601A0A29072C713BCAC3C32C674F9A9EA7019A2DCD77B9B173FB6267B0` |
| [`M8_decision_package.md`](../00_scope/M8_decision_package.md) | 12,857 | `51CD8ED45411EA520B92C28C154DFDD16BA144D26C9927D76E5DC2239D9BE350` |
| [`master_execution_plan.md`](../00_scope/master_execution_plan.md) | 18,200 | `0D664CD467193DF9A0AEC7B81E55C48D63008C5A0C1E17042202D77A4D1E98CE` |
| [`06_reproduction/README.md`](../06_reproduction/README.md) | 996 | `B0B04BC9C5DEE86E7CBE85C93ED87EEAD77E4AB81F0BE96B69FD0CDCC21C985B` |
| [`direct-environment.yml`](../06_reproduction/environment/direct-environment.yml) | 494 | `57D49382584C9A82080436A90564972BC3007E9921848E3CB74A5E701295D491` |
| [`m9_preflight.json`](../06_reproduction/manifests/m9_preflight.json) | 1,023 | `F94B4C69ED888A5BE67917F6F4BFD4CDC64E887409D8341434C3D00F9D53362F` |

项目根目录不是 Git 工作树，因而不能用 Git 提交或差异证明审计输入集合。本表的逐文件 SHA-256 是本次独立审计快照；报告自身的 SHA-256 因自引用会改变文件内容，不写入报告正文，应以交付消息给出的外部哈希为准。

## 3. 已通过的独立核对

### 3.1 冻结源码、依赖候选与 GPU 兼容性边界

[DeepH-pack 官方 `v0.2.2` 标签](https://github.com/mzjb/DeepH-pack/tree/v0.2.2)当前直接指向提交 `66703c532a6f633f4bbc8f94f75c8698a7f89859`，与 D-013、M8 决策包、工作包和版本候选一致。该标签的官方 README 明确列出 Python 3.9、PyTorch 1.9.1、PyTorch Geometric 1.7.2、e3nn 0.3.5 和 Julia 1.6.6；`direct-environment.yml` 没有混入 DeepH-pack `main`、DeepH-E3 或其他材料后端。

公开官方元数据仍提供 Linux x86-64/Python 3.9 的 PyTorch 1.9.1 CUDA 11.1 构件 `pytorch-1.9.1-py3.9_cuda11.1_cudnn8.0.5_0.tar.bz2`，`rusty1s` 仍提供 `pytorch-geometric-1.7.2-py39_torch_1.9.0_cu111.tar.bz2`，[Miniconda 官方目录](https://repo.anaconda.com/miniconda/)仍提供 `Miniconda3-py39_4.10.3-Linux-x86_64.sh`。这只能证明候选构件可获得，不能证明旧 PyTorch/CUDA 二进制已为计算能力 8.9 的 RTX 4080 提供可用内核；PyG 构件名中的 `torch_1.9.0` 与冻结 PyTorch 1.9.1 也必须视为待实际验证的兼容性风险。

工作包 `M9_stageF_work_package.md:62-76` 已要求在失败时保留完整错误并暂停，禁止自行升级 PyTorch、CUDA、PyG 或 DeepH-pack，并要求 CPU/CUDA 张量、GPU 前向与反向、有限梯度、PyG 聚合、e3nn 张量积、DeepH CLI/配置以及两个新进程的一致性测试。该合同足以把“构件可获得”和“栈实际兼容”严格区分；通过本审计不应被解释为兼容性已经通过。

### 3.2 WSL 路径、身份与无凭据落盘边界

只读核验得到：`Ubuntu-22.04` 为 WSL 2，注册表 `BasePath` 为 `E:\Laptop\WSL`，`DefaultUid=1000`；普通 Linux 用户为 `evan-williams`，系统为 Ubuntu 22.04.5 LTS，内核为 `6.18.33.2-microsoft-standard-WSL2`，根文件系统可用空间为 955 GiB。WSL 内 `nvidia-smi` 报告 NVIDIA GeForce RTX 4080 Laptop GPU、12,282 MiB、计算能力 8.9 和驱动 610.88。E 盘审计时可用空间约 367.98 GiB。这些事实与 `m9_preflight.json` 一致；审计结束时发行版保持停止状态。

工作包和复现 README 明确规定项目侧只保存不含凭据的小型清单、配置、验证代码和报告；大文件位于 `/home/evan-williams/deeph-m9`，其 VHDX 注册在 E 盘；需要系统级操作时使用宿主 WSL root 入口，不传递用户口令。认证口令被明确排除在文件、命令行、日志、哈希和报告之外。该边界通过。

### 3.3 数据对象、大小、MD5、URL 与数据契约

[Zenodo 官方记录 `10.5281/zenodo.6555484`](https://zenodo.org/records/6555484)的当前官方 API 元数据显示，冻结对象是 `graphene_dataset.zip`，精确大小为 1,833,785,660 字节，登记校验为 `md5:348c21faacdc62433dd8f01994824916`，精确文件内容 URL 为 [Zenodo API 文件端点](https://zenodo.org/api/records/6555484/files/graphene_dataset.zip/content)。四项均与工作包和 M8 决策包一致。

工作包要求下载后核对字节数、登记 MD5、本地 SHA-256 和 ZIP 完整性，拒绝绝对路径、父目录逃逸、重复规范路径及异常链接，并在安全解压后先冻结只读清单。随后必须核对结构、晶胞、周期维度、Hamiltonian、overlap、basis、单位、局部坐标、截断、配置、划分、标签、参考物理量以及数组的 shape、dtype、有限性、键空间和稀疏模式；任一关键对象不足即按数据契约失败暂停，不得补造数据或静默换源。该数据入口和失败边界通过。

### 3.4 顺序门控、禁止项与审计闭环

M9-01 至 M9-08 的依赖顺序完整：环境与兼容性先于源码烟雾测试，烟雾测试先于正式数据，数据契约先于划分与训练，训练先于矩阵/物理验证，实际证据最后接受新的独立总审计。OpenMX/其他 DFT 安装、正式标签生成、静默切换分支、材料、后端、预算或高级物理范围均被禁止；需要 Windows 重启、兼容性失败、哈希不符、解压逃逸、数据契约不足、NaN/Inf、不可恢复 OOM、泄漏或 basis/overlap 错配时均要求暂停并保留证据。

M9-08 还要求由新的独立审计员核对实际安装对象、数据、代码、日志、自动测试、矩阵/物理结果、预算和授权边界，并由主 agent 修复后交回同一审计员定点复核，直至新增及剩余问题均为 0。该闭环结构通过；本报告的四项阻塞不否定这一结构，只阻止其进入执行。

## 4. BLOCKING

### M9-F-WP-B01：M9 外部动作授权尚未在权威决策与状态链中闭合

**位置与证据。** 工作包 `M9_stageF_work_package.md:8` 声称用户已经明确授权 WSL、依赖、官方 graphene 下载和复现实验，预检 `m9_preflight.json:5-10` 也登记 `m9_external_actions=true`。但 D-013 `decisions.md:126` 明确规定该决策只完成 M8，全部 M9 外部动作仍须再次明确授权；M8 决策包 `M8_decision_package.md:10,123` 重复同一边界；主计划 `master_execution_plan.md:66,82,223` 仍把 M9 状态写为 `AWAITING_EXECUTION_AUTHORIZATION`。因此，当前输入同时存在“已授权”和“等待授权”两个权威状态。由工作包或预检清单自行声明授权，不能消除主决策链的相反状态。

**影响。** 外部动作的允许范围虽然在工作包中写得清楚，但审计输入不能唯一回答授权是否已经生效，也不能证明授权日期、对应工作包快照和禁止项属于同一次明确用户决定。直接启动 M9-02 会违反主计划的二次授权门槛和状态一致性规则。

**最小关闭条件。** 将 D-013 之后取得的明确用户授权登记为新的不可覆盖决策记录，至少包含日期、授权所对应的工作包 SHA-256、允许的 WSL/依赖/DeepH/官方 graphene/复现实验动作、禁止的 OpenMX/其他 DFT 安装与标签生成、禁止自动重启和口令不落盘边界；同步把主计划及复现 README 的 M9 状态改为“已授权但等待 M9-01 审计通过”或等价的唯一状态。不得倒填或推测未实际发生的用户授权。

### M9-F-WP-B02：7 天、24 GPU 小时和 100 GiB 没有统一、可执行的计量与硬停止合同

**位置与证据。** 冻结总预算为 7 天墙钟、24 GPU 小时和 100 GiB，GPU 子预算满足

\[
2\ \mathrm{GPU\,h}+16\ \mathrm{GPU\,h}+6\ \mathrm{GPU\,h}=24\ \mathrm{GPU\,h}.
\]

工作包 `M9_stageF_work_package.md:76,94,110` 分别给出烟雾、训练/验证和物理诊断上限，但没有定义失败运行、OOM、环境验证、受控复跑是否计入哪个桶，也没有定义 GPU 小时的起止时间、并发或跨进程汇总规则。7 天墙钟的起始事件未指定；100 GiB 没有规定统计路径、VHDX 宿主增长与 Linux 逻辑占用的口径、解压前空间预测、定期采样和达到上限时的硬停止动作。统一停止清单 `M9_stageF_work_package.md:120-134` 只对训练子预算给出停止，未把总墙钟和存储上限列为无条件停止项。

**影响。** 即使所有计算结果正确，现有日志也可能无法证明 B2 预算未被超出；约 1.71 GiB 的压缩包不能推出解压、图缓存、检查点、日志和 VHDX 实际增长一定低于 100 GiB。预算是 D-013 的冻结对象，不能在执行中临时解释。

**最小关闭条件。** 在工作包中冻结一个机读预算账本合同：以第一个获授权的变更性 M9 命令作为 7 天墙钟起点；所有占用 GPU 的成功、失败、OOM、烟雾、训练、验证、复跑、推理和诊断进程均按单卡实际占用区间累计，并唯一归入 2/16/6 三个桶；开始和结束时记录 UTC 时间、PID、命令/配置哈希及累计值。100 GiB 同时按 `/home/evan-williams/deeph-m9` 逻辑占用和宿主 VHDX 增量记录，下载、解压、图生成和训练前均执行预测与实测检查。把 7 天、24 GPU 小时、各子预算和 100 GiB 全部加入统一停止清单，达到或预计下一原子操作将超过任一上限时保存证据并停止。

### M9-F-WP-B03：阶段 F 要求的适用旋转、反演或时间反演验收未进入 M9-06

**位置与证据。** 主计划 `master_execution_plan.md:244-255` 将适用的旋转、反演或时间反演残差列为阶段 F 最低自动检查。工作包 M9-06 `M9_stageF_work_package.md:96-110` 覆盖 Hermiticity、逆边、平移、overlap、矩阵误差、实空间到 (k) 空间重建、广义本征残差、能带、DOS、简并和本征态指标，却没有对旋转、反演或时间反演作适用性判定、输入配对、变换约定、残差定义或失败阈值。graphene 被冻结为非磁、无 SOC，并不能据此把所有这些检查自动判为不适用；至少应显式论证并测试适用的自旋无关时间反演关系，例如

\[
H(\mathbf{k})=H(-\mathbf{k})^{*},\qquad
S(\mathbf{k})=S(-\mathbf{k})^{*},
\]

以及相应的广义谱对称性，具体形式仍取决于冻结的基、相位和 (k) 点配对约定。

**影响。** 现有 M9-06 即使全部执行，也不能满足主计划“矩阵、对称性和下游物理量同时通过”的完成门槛；标题中的“对称性”不能代替可执行残差和失败判据。

**最小关闭条件。** 在 M9-04 数据契约中加入旋转、反演、时间反演三类验收的逐项适用性判定及所需对象；在 M9-06 对每个适用项冻结变换方向、基/相位约定、配对键、规范不变量、残差、精度尺度和失败样例。对冻结的非磁无 SOC graphene，至少应实现基于成对 (\mathbf{k}) 点的 (H/S) 时间反演残差与广义谱残差；若旋转或反演因官方归档缺少合法配对而不可执行，应提供可审计证据，并由修订后的阶段门控明确说明这是否触发数据契约失败，不得以“不可评估”静默替代主计划最低项。

### M9-F-WP-B04：数值容差冻结时点弱于 D-013 的训练前预登记要求

**位置与证据。** M8 决策包 `M8_decision_package.md:98` 规定具体数值容差必须在下载后、训练前根据官方配置和数据精度登记，不得在看到测试结果后放宽。D-013 `decisions.md:125` 也要求具体容差从固定数据与配置提取或在看到测试结果前登记。工作包 `M9_stageF_work_package.md:110` 却只要求在“查看最终测试结果前”登记。该文字允许在训练、验证、试推理或中间诊断结果已经可见后再设阈值，实质放宽了冻结决策。

**影响。** 矩阵 MAE、Hermiticity、(S\succ0)、条件数、广义本征残差、能带/DOS/简并及本征态指标的阈值可能受已观察结果影响，导致选择性验收；此时即使最终测试集未被直接查看，也不能证明阈值是预登记的。

**最小关闭条件。** 将 M9-06 的阈值冻结点恢复为“完成 M9-04 数据清单和精度/官方配置核对之后、任何 M9-05 训练或使用模型输出的验证、试推理和诊断之前”。阈值清单应含指标定义、归一化、聚合、单位、适用子集、绝对/相对容差、依据来源、生成时间和 SHA-256；冻结后只允许因书写或实现错误通过新增决策修订，且必须保留旧值和修订理由，不能依据已观察模型结果放宽。

## 5. NON_BLOCKING

`NON_BLOCKING=0`。本轮没有登记措辞或便利性建议。依赖候选的实际兼容性、官方数据内部对象是否足够、合法 OOD 是否可评估及最终数值容差都属于后续门控必须用真实对象回答的问题；工作包已经为这些未知项设置“验证失败即暂停”的原则，因此不应在本审计中把未知事实伪装为已知缺陷或已通过结果。

## 6. 门控覆盖汇总

| 审计对象 | 判定 | 依据与边界 |
|---|---|---|
| WSL 发行版、注册路径、Linux 用户、GPU | `PASS` | 本机只读事实与预检一致；不包含任何口令读取 |
| DeepH 标签与提交 | `PASS` | `v0.2.2` 当前直接指向固定提交 |
| 直接依赖候选与可获得性 | `PASS/待运行门控` | 构件仍可获得；不等于 `sm_89` 兼容，现有失败暂停合同充分 |
| M9 外部动作授权 | `FAIL` | B01：决策链与主计划仍显示等待授权 |
| OpenMX/DFT、标签和范围禁止项 | `PASS` | 禁止项明确，失败后不得静默换后端或换版本 |
| 7 天、24 GPU 小时、100 GiB | `FAIL` | B02：算术一致，但计量、归桶和硬停止不可执行 |
| 数据文件、大小、MD5、URL | `PASS` | 与 Zenodo 官方元数据一致；本地 SHA-256 必须在实际下载后新增 |
| ZIP 安全与数据契约 | `PASS/待真实对象验证` | 检查项和失败暂停边界充分，不预判归档内容 |
| 划分、随机性和泄漏 | `PASS` | 官方显式划分优先；否则冻结结构分组 80/10/10 ID 清单及哈希 |
| 矩阵、(k) 空间和广义本征 | `PASS/待修订阈值时点` | 对象覆盖充分；B04 要求训练前冻结阈值 |
| 旋转、反演或时间反演 | `FAIL` | B03：主计划最低项未进入工作包的可执行验收 |
| 物理量与成本 | `PASS/待真实对象验证` | 全能带、窗口、带隙、带宽、DOS、简并、本征态和分项成本均已列出 |
| 停止条件与独立总审计闭环 | `FAIL/结构通过` | 审计循环结构通过；B02 的总墙钟与存储停止项缺失 |

## 7. 最终门控意见与最小关闭集合

当前不得启动 M9-02。解除本门控不要求更换已经冻结的 DeepH-pack 版本、graphene 数据、WSL 路径、GPU、依赖候选或物理范围，也不要求安装或下载任何对象。最小关闭集合仅包含：

- 按 B01 建立 D-013 之后的明确 M9 授权记录，并统一主计划和复现 README 状态；
- 按 B02 冻结墙钟、GPU 和存储的机读计量、归桶、预测检查与硬停止合同；
- 按 B03 增加适用旋转、反演、时间反演的判定和可执行残差，至少覆盖非磁无 SOC graphene 的时间反演 (H/S) 与广义谱关系；
- 按 B04 把全部验收容差恢复为数据核对后、任何训练和模型输出观察前冻结。

主 agent 完成上述局部修订后，应把受影响文件及新授权记录的 SHA-256 交回同一独立审计员。只有定点复核明确给出 `PASS`、`BLOCKING=0`、`NON_BLOCKING=0`，才允许启动 M9-02；M9-02 的允许也仅表示可以执行环境安装与兼容性门控，不表示旧 CUDA 栈、官方数据契约、训练、矩阵验收或物理结果已经通过。

## 8. 报告自身验证方法

本报告应在最终内容固定后执行以下验证：UTF-8 非 CR/LF/TAB 控制字符扫描；活动本地 Markdown 链接存在性检查；HTTP(S) 链接只读可达性检查；Pandoc 3.6.4 使用 `markdown+tex_math_single_backslash`、`--fail-if-warnings` 和 HTML5 MathML 的严格转换；检查输出中实际存在 `<math>` 节点；最后计算报告文件 SHA-256。具体退出码、计数和最终哈希以交付消息中的独立验证结果为准。
