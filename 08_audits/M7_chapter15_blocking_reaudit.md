# M7-02 第 15 章第一次定点复核

## 1. 复核结论

**结论：FAIL**

- 原问题：`M7-02-B01`—`M7-02-B04` 与 `M7-02-N01` 均为 `CLOSED`。
- 新增问题：1 项。
- `BLOCKING = 0`。
- `NON_BLOCKING = 1`。
- **不允许**将 M7-02 标记为 `COMPLETED`。
- **不允许**启动 M7-03。

主 agent 已关闭首次审计的全部稳定问题，但修复 B01 时新增的一行距离回归说明没有使用数学定界符。严格 Pandoc 虽返回成功，却把 `\mathsf` 从普通文本中剥离，未生成 MathML。该问题不推翻已修正的 `\sqrt{26}` 数学结论，故列为非阻塞项；但 M7-02 的门控明确要求独立审计问题为 0，因此本轮仍不能通过。

## 2. 复核范围与方法

本轮只读复核了首次报告的完整送审范围及修订后的相邻内容，未修改任何教材、推导、题解、来源、计划或台账。独立执行：

- 7 个材料文件的 `markdown+tex_math_single_backslash` 到 HTML5/MathML 转换；
- 活动本地链接、C0 控制字符、Q15/A15 配对、唯一性和非空检查；
- 距离平方、错转置残差、随机旋转正交性、行列式、二阶张量、叉积宇称和行晶格桥接复算；
- 阶段 E 11 个来源快照哈希与 7 个必需 BibTeX 键复核；
- 旧问题逐项定位及全文普通文本 TeX 命令扫描。

7 个文件严格 Pandoc 全部返回 0，共生成 409 个 MathML 节点；13 个活动本地链接零断链；控制字符错误数为 0。Q15-01—Q15-10 与 A15-01—A15-10 各 10 个，按序一一对应、唯一且非空。

## 3. 原问题定点复核

### M7-02-B01：CLOSED

`examples.md:91-100` 已将距离改为

\[
\lVert u-v\rVert_2=\sqrt{26}.
\]

独立复算得到旋转前后平方距离均为 26，距离为 `5.0990195135927845`。`15_group_actions_equivariance.md` 第 9、13 节也加入了平方距离 26 的正向锚点和错转置失败锚点。错转置残差独立复算为

\[
\rho=\sqrt{5/7}=0.8451542547285166.
\]

原错误数值已消失，相邻正文、题面和解答未发现仍引用 `\sqrt{21}` 作为该距离。

### M7-02-B02：CLOSED

`15_group_actions_equivariance.md` 已新增：

- 第 2 节符号、维度与适用条件表；
- 第 9 节完整数值正例和定量失败例；
- 第 12 节 D-E01/D-E09 到 T-E01、T-E02、T-E07、T-E10 的显式映射；
- 本章只完成 D-E09 残差、精度和成本接口，A/B、144 点扫描、完整推导汇总及自动执行分别留待 M7-08/M7-09 的边界。

输入/输出 shape、有限性、dtype、轨道块维度及未执行对象均已明确，不再把接口式外推为完整验证。冻结推导结构契约已经满足。

### M7-02-B03：CLOSED

`sources.md` 已将“待正文逐项定位”替换为已完成的来源—论断定位表，给出 E-FND-03、E-GNN-01、E-GNN-02、DH-02、阶段 D、D-E01/D-E09 和 T-E 入口的正文、推导或题解位置。DH-02 已加入直接来源表，并明确限制为论文层 Hamiltonian 块方法关系，不构成软件、仓库、数据、材料、DFT、SOC 实践或复现授权。

阶段 E 清单中的 11 个快照实际 SHA-256 与登记值全部一致。`bibliography.bib` 共 46 个唯一键、花括号平衡为 0；`dlmf1430`、`dlmf34`、`turner2017quantum321`、`thomas2018tfn`、`geiger2022e3nn`、`gong2023deephe3`、`rudner2009solids2lec10` 均恰有一项。

### M7-02-B04：CLOSED

正文 `15.8.4` 已新增章节小结，收束：

- 主动/被动和 `R_2R_1` 复合；
- `SO(3)`/`O(3)`/`E(3)` 范围；
- 标量、极向量、轴向量和张量的表示区别；
- 行晶格、周期 shift、完整边键、置换和换胞边界；
- dtype、残差和失败夹具；
- Hamiltonian 块协变及第 16 章 `s,p,d` 表接口。

提纲同步新增 `15.8.4`，门控与授权边界仍单独保留。

### M7-02-N01：CLOSED

正文 `chapter.md:434-436` 已将 `R`、`\det R`、`SO(3)` 和 `O(3)` 全部置于有效的 `\(...\)` 数学定界符内。对应内容均生成 MathML。

## 4. 新增 NON_BLOCKING 问题

### M7-02-R1-N01：例题 3 新增的距离回归说明未使用数学定界符

**位置：** `03_textbook/chapters/15_geometric_transformations_equivariance/examples.md:98`。

**证据：** 当前文本为：

> 其中 `(u-v=(-1,3,-4)^{\mathsf T})`，故平方距离严格为 `(1+9+16=26)`。

两段数学对象均为普通圆括号，而非 `\(...\)`。严格 Pandoc 输出实际变为普通 HTML 文本 `(u-v=(-1,3,-4)^{T})` 和 `(1+9+16=26)`；`\mathsf` 命令被剥离，整段没有 MathML。数学值 26 已由相邻 display math 和独立复算正确支持，因此该问题不推翻 B01 的关闭结论，但它是修复新增的渲染回归，且该句自称“直接检查”，应当具有可验证数学语义。

**最小关闭条件：** 将该行两段分别写成有效数学形式，例如 `\(u-v=(-1,3,-4)^{\mathsf T}\)` 与 `\(1+9+16=26\)`；重新运行严格 Pandoc/MathML，并对普通文本 TeX 命令扫描确认无残留。修复后交回同一审计员执行第二次定点复核。

## 5. 相邻回归与授权边界

250 组独立随机旋转复算得到：

- 最大正交残差 `2.11e-15`；
- 最大行列式偏差 `1.56e-15`；
- 二阶张量 Frobenius 范数最大偏差 `4.00e-15`；
- 叉积 `SO(3)` 协变最大绝对残差 `3.83e-15`；
- 行晶格桥接最大绝对残差 `2.04e-15`；
- 反射下轴向叉积残差 0。

这些结果与冻结公式一致，未发现因本轮修复引入其他数学、来源或链接回归。材料继续禁止 DeepH/e3nn 安装、正式数据、DFT 标签及 M8 实践对象的隐含选择；M8 决策冻结和 M9 二次执行授权边界保持完整。

## 6. 后续门控

主 agent 应只修复 `M7-02-R1-N01`，不得自行关闭。修复后由本审计员执行第二次定点复核，并同时检查新增问题和全部旧问题回归。

只有复核达到 `PASS`、`BLOCKING=0`、`NON_BLOCKING=0` 且明确允许时，才可将 M7-02 标记为 `COMPLETED` 并启动 M7-03。

## 7. 报告完整性

报告自身的严格 Pandoc/MathML、活动本地链接和控制字符检查在写入后独立执行；稳定 SHA-256 由交付回报提供，避免在报告正文内形成自指哈希。
